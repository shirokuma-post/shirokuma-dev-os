> Tier: T0 ・ 最終確認日: {{YYYY-MM-DD}} ・ 1行責務: このプロジェクトの最上位ガイド（北極星 + 指揮系統 + ルールへの入口）

# {{プロジェクト名}} — 指揮系統ガイド

> **このファイルは参謀（Orchestrator）用。軽量に保つこと。**
> - まず §0(北極星) を読む — 全判断の最上位
> - 不変ルール → `INVARIANTS.md`（セキュリティゲートのチェックリスト源）
> - 文書運用ルール → `DOC_CONSTITUTION.md`
> - 思考様式・参謀フローの本体 → プラグイン `shirokuma-dev-os` のスキル群

---

## §0. 北極星（WHY）

> このシステムが**何のために在るか**。全ての判断の最上位。機能・UI・掃除はこの下に乗るだけ。

- **北極星**: {{このプロダクトは誰の・何のために在るか（1〜2文）}}
- **幹（構造の核）**: {{このシステムで一番だいじな構造・境界線は何か}}
- **危険度の測り方**: 変更の危険度は「{{どの境界を侵すか}}」で測る。境界に無関係な掃除は最低優先（→ `engineering-doctrine` 規律4）。

---

## デフォルト動作: 参謀モード

**タスクを受けたら、自動的にスキル `staff-officer` の指揮系統で動く。**

- 「〜して」「〜直して」「〜追加して」= 参謀モード発動（タスク分解 → 並列 Worker → 検品 → セキュリティゲート → 統合）
- 質問・相談・雑談には普通に答える（dispatch しない）
- 小タスク（1ファイル修正）でも最低限の実況報告と作業ログは残す

> 詳細フローは スキル `staff-officer`。思考の判断軸は スキル `engineering-doctrine`。

---

## 組織体制（5層 × 4ライン）— サマリ

```
Layer 0: 依頼者({{あなた}})   意思決定・承認
Layer 1: 参謀                タスク分解 → 配備 → 統合報告。コードは書かない
Layer 2: Worker(並列)        UI / ロジック / 基盤 / Ops。worktree 隔離で実装
Layer 3: 検品(QA)            Worker とは別 Agent が差分を精査
Layer 3.5: セキュリティゲート 🔒 全 diff 横断で INVARIANTS.md 照合。拒否権あり
Layer 4: 統合                検品 + ゲート通過後のみマージ・報告
```

| ライン | 担当 | このプロジェクトでの監視/対象 |
|---|---|---|
| **UI/UX** | 画面・スタイル・レスポンシブ | {{デザインシステムの正典パス}} |
| **ロジック** | API・ビジネスロジック・暗号・認証・AI | {{詳細仕様のパス}} |
| **基盤** | DB migration・権限・index・deploy・env | {{migrations のパス}} |
| **Ops** | ヘルス・監視・状態確認（読み取り専用） | {{ヘルスチェック経路 / 監視基盤}} |

---

## §1. 不変ルール

→ **`INVARIANTS.md`** を参照（セキュリティ核 / 性能核 / DB アクセス / デプロイ / 入力検証 / 依存管理 / 責任境界）。
セキュリティゲートはそれをチェックリストとして使う。

---

## §2. プロダクト/モジュール体系

{{このプロジェクトを構成するサービス・モジュールの一覧（名前・役割・URL・ステータス）}}

### 技術スタック

{{言語 / フレームワーク / DB / 認証 / AI / 決済 / インフラ など}}

---

## ファイル案内

| ファイル | 用途 |
|---|---|
| `INVARIANTS.md` | 不変ルール（セキュリティゲートの源） |
| `DOC_CONSTITUTION.md` | 文書運用ルール |
| {{詳細仕様}} | {{Worker / 検品官が読む詳細}} |
| {{デザインシステム}} | {{UI Worker / UI 検品官が読む規範}} |

> プラグイン `shirokuma-dev-os` のスキル（engineering-doctrine / doc-constitution / staff-officer）が、
> このプロジェクトでも自動で発動する。固有の値・パスだけ上の `{{}}` に埋めれば指揮系統が動く。
