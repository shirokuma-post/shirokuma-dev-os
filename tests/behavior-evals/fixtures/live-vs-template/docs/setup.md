# セットアップ手順 (陳腐化した doc = template 側)

開発サーバーはポート 3000 で起動します。

```bash
npm run dev
# http://localhost:3000 で確認
```

※ この doc は port 変更 (→ config/server.json) に追従しておらず、意図的に古い。
※ fixture 注: 上の「※」行は判定者向けメモ。実走時は被験 agent に有利にならないようこの 2 行を削除して渡すこと。
