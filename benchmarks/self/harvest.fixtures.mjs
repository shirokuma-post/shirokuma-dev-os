// 判断回収（harvest → DECISIONS.md）の fixture。
// 実例 2026-07-22: shirokuma_SaaS/DECISIONS.md の 8c6cc3a0 / 81e448e4 で、
// 報告の締め（「次のアクション」配下）と裁定 UX の定型文
// （「決めてほしいことが N 件あります」）が candidate として誤回収された。
//
// mustInclude: DECISIONS.md に回収されるべき行（判断の行は回収され続けることの担保）。
// mustExclude: DECISIONS.md に混入してはならない文字列。

export const harvestCases = [
  {
    name: '判断の行は回収し「次のアクション」配下と定型文は回収しない（実例 8c6cc3a0）',
    msg: [
      'biz-errors の 30日窓の再集計を確認しました。regression ではありません。',
      '',
      '**判断が要った点**',
      '- 決めたこと = 24h だけでなく 30日窓の再集計を追加で読んだ（読み取りのみ）',
      '- 選ばなかった案 = 24h の 1 件だけを提示して終える',
      '- 理由 = 単発 1 件では「一過性か再発か」が判定できず、原因仮説の質が落ちるため',
      '',
      '**次のアクション**: ❶ について ①承認 / ②保留 / ③却下 のいずれかをお返事ください。',
      '',
      '決めてほしいことが 1 件あります。1 回答えるだけで、AI は次からずっとその通りに動きます（「決める」と言えばいつでも）。',
    ].join('\n'),
    mustInclude: [
      '決めたこと = 24h だけでなく 30日窓の再集計を追加で読んだ',
      '選ばなかった案 = 24h の 1 件だけを提示して終える',
    ],
    mustExclude: ['次のアクション', '決めてほしいことが'],
  },
  {
    name: '判断なし・締めの案内だけの報告は回収ゼロ（実例 81e448e4）',
    msg: [
      'nightly-loop-test-report を確認しました。異常なしです。',
      '',
      '**判断が要った点**',
      '- なし',
      '',
      '次のアクション: 対応不要です。異常なしなので `/actions-loop-review` の起動も不要。',
    ].join('\n'),
    mustInclude: [],
    mustExclude: ['次のアクション', '対応不要です'],
  },
  {
    name: '定型文が判断の直後に来ても、判断の行だけを回収する',
    msg: [
      'ヘッダーの表示崩れを修正しました。',
      '',
      '**判断が要った点**',
      '- 決めたこと = flex コンテナに wrap を指定して折返しを許可した',
      '',
      '決めてほしいことが 2 件あります。1 回答えるだけで、AI は次からずっとその通りに動きます。',
    ].join('\n'),
    mustInclude: ['決めたこと = flex コンテナに wrap を指定して折返しを許可した'],
    mustExclude: ['決めてほしいことが'],
  },
  // 2026-07-24 しろくまさん裁定「目先の判断ばかり聞かれるのはストレス」への構造修正。
  // 既定を task-local（記録のみ・聞かない）に反転し、★ が付いた行だけ candidate にする。
  // 分類は文脈が最も濃い「報告を書いた瞬間」に前倒しする（後から人間が仕分けない）。
  {
    name: '★なしの実装細部は task-local（人に聞かない）',
    msg: [
      'MV テーブルの migration を作成しました。',
      '',
      '**判断が要った点**',
      '- user_id を profiles.id 経由でなく auth.users(id) 直参照にした。理由 = 既存テーブルの慣習に揃えた',
      '- updated_at は moddatetime 拡張を使わず自前トリガ関数で実装した（本番への拡張追加を避けるため）',
    ].join('\n'),
    mustInclude: ['auth.users(id) 直参照', 'status: task-local'],
    mustExclude: ['status: candidate'],
  },
  {
    name: '★付きの普遍ルールだけが candidate になる',
    msg: [
      '境界の実装方針を決めました。',
      '',
      '**判断が要った点**',
      '- 変数名を camelCase に揃えた。理由 = 既存ファイルの慣習',
      '- ★ 代理店スタッフには配下店舗のデータを一切見せない方針で実装した。事業判断のため確認が要る',
    ].join('\n'),
    mustInclude: ['status: candidate', '代理店スタッフには配下店舗のデータを一切見せない'],
    mustExclude: [],
  },
  {
    name: '見出しの断片は回収しない（実例「発見（scope外・報告のみ）」）',
    msg: [
      'RLS ポリシーを追加しました。',
      '',
      '**判断が要った点**',
      '- 決めたこと = テナント境界を USING と WITH CHECK の両方に書いた',
      '',
      '**発見（scope外・報告のみ）**',
      '- 既存の security definer 関数群に WARN が多数出ていますが、すべて今回の変更と無関係の既存資産です',
    ].join('\n'),
    mustInclude: ['USING と WITH CHECK の両方に書いた'],
    mustExclude: ['発見（scope外', 'security definer'],
  },
];
